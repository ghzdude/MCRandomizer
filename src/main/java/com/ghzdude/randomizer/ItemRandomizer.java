package com.ghzdude.randomizer;

/* Item Randomizer
 * Goal is to give the player a random item every so often
 * Every so often, points are added to a counter
 * those points are then used to give the player an item
 * more points give bigger stacksize of item at once
 * items have a defined value, otherwise stacksize is used
 */

public class ItemRandomizer {
}
