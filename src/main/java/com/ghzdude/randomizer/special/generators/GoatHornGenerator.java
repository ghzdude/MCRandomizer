package com.ghzdude.randomizer.special.generators;

import com.ghzdude.randomizer.RandomizerCore;
import net.minecraft.core.Registry;
import net.minecraft.nbt.CompoundTag;
import net.minecraft.resources.ResourceLocation;
import net.minecraft.world.item.*;

import java.util.ArrayList;

public class GoatHornGenerator {
//    private static ArrayList<ResourceLocation> instrumentList = new ArrayList<>(Registry.INSTRUMENT.keySet());
//    public static void applyGoatHornSound(ItemStack stack) {
//        CompoundTag baseTag = new CompoundTag();
//        int id = RandomizerCore.RANDOM.nextInt(instrumentList.size());
//        baseTag.putString("instrument", instrumentList.get(id).toString());
//        stack.setTag(baseTag);
//    }
}
